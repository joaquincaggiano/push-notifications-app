import { View } from "react-native";
import { ThemedText } from "@/components/ThemedText";

const PushNotificationApp = () => {
  return (
    <View>
      <ThemedText>PushNotificationApp</ThemedText>
    </View>
  );
};

export default PushNotificationApp;
